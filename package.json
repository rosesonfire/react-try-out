{
  "name": "pa",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel-clean": "rm -rf dist && mkdir dist",
    "babel-build": "babel src -s -d dist",
    "babel-build-with-watcher": "babel src --watch -s -d dist",
    "node-start-with-watcher": "nodemon --watch dist dist/main/server",
    "start-server": "npm run babel-clean && npm run babel-build && npm run node-start-with-watcher & npm run babel-build-with-watcher"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "bootstrap": "^4.0.0-beta",
    "express": "^4.15.4",
    "node": "^8.3.0",
    "nodemon": "^1.12.1",
    "react": "^16.0.0",
    "react-dom": "^16.0.0"
  }
}
