"use strict";

module.exports = {
    app: {
        server: {
            host: "localhost",
            port: 8080
        }
    },
    db: {
        connection: {
            host: "localhost",
            port: 27017,
            dbName: "pa-db"
        }
    }
}